<%= form_for(@phone_provider) do |f| %>
  <% if @phone_provider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone_provider.errors.count, "error") %> prohibited this phone_provider from being saved:</h2>

      <ul>
      <% @phone_provider.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :phone_provider %><br />
    <%= f.text_field :phone_provider %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
