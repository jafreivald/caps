<%= form_for(@role_definition) do |f| %>
  <% if @role_definition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role_definition.errors.count, "error") %> prohibited this role_definition from being saved:</h2>

      <ul>
      <% @role_definition.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :role %><br />
    <%= f.text_field :role %>
  </div>
  <div class="field">
    <%= f.label :profile %><br />
    <%= f.text_field :profile %>
  </div>
  <div class="field">
    <%= f.label :resource_utilization %><br />
    <%= f.text_field :resource_utilization %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
