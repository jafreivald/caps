<%= form_for(@contact_number) do |f| %>
  <% if @contact_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact_number.errors.count, "error") %> prohibited this contact_number from being saved:</h2>

      <ul>
      <% @contact_number.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :preferred %><br />
    <%= f.check_box :preferred, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :contact_method %><br />
    <%= f.text_field :contact_method, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :phone_number %><br />
    <%= f.text_field :phone_number, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :profile %><br />
    <%= f.text_field :profile, :class => "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit :class => "submit_record btn btn-primary" %>
  </div>
<% end %>
